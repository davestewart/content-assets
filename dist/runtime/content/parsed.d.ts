import type { AssetConfig, ParsedContent } from '../../types';
/**
 * Rewrite cached content with updated image sizes
 */
export declare function rewriteContent(path: string, asset: AssetConfig): ParsedContent;
